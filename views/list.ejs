<%- include("header"); -%>
    <div class="box" id="heading">
        <h1><%= ListTitle %> </h1>
    </div>

    <!-- <% if(ListTitle==="Saturday"||ListTitle==="Sunday"){J %>

      <h1 style="color:green">Its a <%= ListTitle %> </h1>
    <% }else{ %>
      <h1 style="color:orange">Its a <%= ListTitle %> </h1>
      <p>you should be working now </p>
    <% } %> -->
  <div class="box">
    <ul>

    <%newListItems.forEach(function(item){%>
      <form class="" action="/delete" method="post">
        <div class="item">
          <input type="checkbox" name ="checkbox" value="<%=item.id%>" onChange="this.form.submit()">
           <p><%=  item.name %></p>
        </div>
        <input type="hidden" name="listName" value="<%= ListTitle %>"></input>
      </form>
    <%})%>

     <form class="item" action="/" method="post">
       <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
       <button type="submit" name="list" value="<%= ListTitle %>">+</button>
     </form>

  </div>

<%- include("footer"); -%>
